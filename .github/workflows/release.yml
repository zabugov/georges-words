name: Release DMG

# Builds a distributable DMG. Runs on demand (Actions tab → Run workflow)
# or on a version tag push. Self-contained on purpose: it does NOT touch
# build.sh or the local dev flow.
#
# Until a Developer ID exists (backlog 7.1), the DMG is ad-hoc signed —
# fine for our own Macs, but other Macs will show Gatekeeper's
# "unidentified developer" flow. Once the Apple Developer account exists,
# add the two commented steps below (real signing + notarization) and
# the same pipeline produces a drag-and-drop-installable DMG.

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  dmg:
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Cache SwiftPM
        uses: actions/cache@v4
        with:
          path: |
            app/.build
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-${{ hashFiles('app/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-

      - name: Build (release)
        working-directory: app
        run: swift build -c release

      - name: Assemble app bundle
        run: |
          APP="dist/GeorgesWords.app"
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          cp app/.build/release/GeorgesWords "$APP/Contents/MacOS/GeorgesWords"
          cp app/Info.plist "$APP/Contents/Info.plist"
          cp app/icon/AppIcon.icns "$APP/Contents/Resources/AppIcon.icns"

      # ---- Developer ID signing (enable after backlog 7.1) ----
      # Needs repo secrets: MACOS_CERT_P12 (base64), MACOS_CERT_PASSWORD.
      # - name: Import signing certificate
      #   run: |
      #     echo "$MACOS_CERT_P12" | base64 --decode > cert.p12
      #     security create-keychain -p ci build.keychain
      #     security default-keychain -s build.keychain
      #     security unlock-keychain -p ci build.keychain
      #     security import cert.p12 -k build.keychain -P "$MACOS_CERT_PASSWORD" -T /usr/bin/codesign
      #     security set-key-partition-list -S apple-tool:,apple: -s -k ci build.keychain

      - name: Sign (ad-hoc until 7.1)
        run: codesign --force --deep --sign - dist/GeorgesWords.app

      - name: Package DMG
        run: |
          VERSION="$(/usr/libexec/PlistBuddy -c 'Print CFBundleShortVersionString' app/Info.plist)"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          mkdir -p dist/dmg
          cp -R dist/GeorgesWords.app dist/dmg/
          ln -s /Applications dist/dmg/Applications
          hdiutil create -volname "George's Words" -srcfolder dist/dmg -ov -format UDZO "dist/GeorgesWords-$VERSION.dmg"

      # ---- Notarization (enable after backlog 7.1) ----
      # Needs secrets: APPLE_ID, APPLE_TEAM_ID, APPLE_APP_PASSWORD.
      # - name: Notarize and staple
      #   run: |
      #     xcrun notarytool submit "dist/GeorgesWords-$VERSION.dmg" \
      #       --apple-id "$APPLE_ID" --team-id "$APPLE_TEAM_ID" \
      #       --password "$APPLE_APP_PASSWORD" --wait
      #     xcrun stapler staple "dist/GeorgesWords-$VERSION.dmg"

      - name: Verify bundle structure
        run: |
          test -x dist/GeorgesWords.app/Contents/MacOS/GeorgesWords
          test -f dist/GeorgesWords.app/Contents/Resources/AppIcon.icns
          codesign -dv dist/GeorgesWords.app
          hdiutil verify "dist/GeorgesWords-$VERSION.dmg"

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: GeorgesWords-${{ env.VERSION }}-dmg
          path: dist/GeorgesWords-*.dmg
          if-no-files-found: error
